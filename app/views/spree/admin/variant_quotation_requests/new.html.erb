<% content_for :page_title do %>
  <%= link_to 'Variant Quotation Request', spree.admin_variant_quotation_requests_url %> /
  <%= Spree.t(:new_variant_quotation_request) %>
<% end %>
<%= form_for [:admin, @variant_quotation_request] do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'spree/admin/shared/new_resource_links' %>
  </fieldset>
<% end %>
